# Docs out of sync: README/UPDATE omit the queue subsystem; status/ describes a never-shipped layout; INSTALL pins a stale ref (ARCH-6/7/8, VER-5)

## Acceptance gate
Issue #81 is resolved on `main` and verified — tests + lint green and the behaviour change confirmed. Close #81 when done.

## Notes
Tracked as GitHub issue #81 (2026-06-20 whole-system review backlog).
